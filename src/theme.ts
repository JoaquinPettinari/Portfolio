import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#191919'
    },
    secondary: {
      main: '#FFFFFF'
    }
  },
  typography: {}
})

export default theme
