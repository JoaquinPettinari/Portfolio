import { createTheme } from "@mui/material"

const theme = createTheme({
    palette: {
      primary: {
        main: '#4890E2',
        dark: '#0068b5',
      },
    },
    typography: {
    }
});

export default theme